# S42 Production Suite — Dependencies v4.0
# ==========================================
#
# Windows ComfyUI Portable:
#   .\python_embeded\python.exe -m pip install -r custom_nodes\S42-Production-Suite\requirements.txt
#
# Linux/Mac ComfyUI Portable:
#   ./python_embeds/bin/python -m pip install -r custom_nodes/S42-Production-Suite/requirements.txt
#
# All audio DSP runs on CPU — GPU stays free for generation.

# ── REQUIRED ────────────────────────────────────────────────────────────────

# Signal processing (filtering, resampling, FFT)
scipy>=1.11.0

# Numerical (already in ComfyUI, listed for completeness)
numpy>=1.24.0

# OpenCV — required for Background Remover, Audio Reactive FX, Layer Composer
opencv-python>=4.8.0

# ── STRONGLY RECOMMENDED ────────────────────────────────────────────────────

# LUFS / EBU R128 loudness measurement (Mastering Chain, Audio Analyser)
pyloudnorm>=0.1.1

# Beat/tempo/onset detection (Beat Analyzer, Audio Analyser, Audio Reactive FX)
librosa>=0.10.0

# ── SONGWRITER — OLLAMA INTEGRATION ─────────────────────────────────────────
#
# S42P Songwriter requires the official Ollama Python client.
# Install separately:
#   pip install ollama
#
# The Songwriter node uses OLLAMA_CONNECTIVITY from ComfyUI-Ollama.
# See README.md for full Ollama setup instructions.
#
# ollama>=0.3.0

# ── AI BACKGROUND REMOVAL (optional) ────────────────────────────────────────
#
# For BiRefNet, BEN2, U2Net, ISNet AI methods:
#   pip install rembg onnxruntime
#
# GPU acceleration for rembg:
#   pip install onnxruntime-gpu
#
# BiRefNet via Transformers (highest quality):
#   pip install transformers torchvision
#
# BEN2 model:
#   pip install ben2
#
# Without any of the above, Background Remover falls back to
# Chroma Key / Luma Key (OpenCV only — no extra install needed).

# ── ALREADY IN COMFYUI ───────────────────────────────────────────────────────
# torch        — included with ComfyUI
# torchaudio   — included with ComfyUI
# Pillow       — included with ComfyUI

# ── CHANGELOG ────────────────────────────────────────────────────────────────
# v4.0  Removed s42p_ollama_nodes (use ComfyUI-Ollama instead)
#       Expanded Procedural FX from 10 to 16 nodes
#       Added ollama pip note for Songwriter
# v3.0  Added opencv-python
# v2.1  Added pyloudnorm, librosa
# v1.0  Initial: scipy, numpy
